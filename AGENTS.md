# Rabbit Hole 작업 하네스

## 제품 불변 조건
- 전체 화면은 하나의 React Flow 캔버스. 일반 에이전트의 공개 응답을 요청별 노드로 스트리밍 표시. 기존 페이지 기록은 읽기 전용으로 복원.
- 페이지 단위 출처 ID 유지. 도메인 병합, 가짜 출처/가격, 실패 시 샘플 대체 금지.
- 내용 관계는 근거가 필요. 응답 노드 사이의 화살표는 대화 순서를 표시하며 내용 관계/인용 근거와 별도 타입으로 구분. 내부 추론을 그래프로 노출하지 않음.
- 사용자의 위치와 viewport 유지. 기록 재개는 유료 호출 없이 IndexedDB에서 복원.

- 주제 키워드에 따른 전용 폼·조건 분기 금지. 필요한 추가 질문도 일반 응답으로 표시.

## 구조 및 인터페이스
- `src/frontend`: React + Vite + TypeScript. `src/backend`: FastAPI + Agents SDK.
- DB는 브라우저 IndexedDB. 외부 DB/로그인/소켓은 현 범위에 없음.
- API 계약은 `docs/API.md`. 변경 시 양쪽 타입과 계약 테스트 함께 갱신.
- 모든 설정과 비밀은 `src/backend/.env` 또는 배포 서버 환경변수. 프런트는 동일 origin `/api` 사용.
- 일반 에이전트는 사용자 요청에 필요한 계산기·웹 검색·페이지 읽기 도구만 예산 내에서 사용. 모든 답변에 일괄 검색하거나 별도 근거 검증·관계 생성 호출 금지. 검색 결과와 본문 조회를 구분하며 생성한 링크나 조회한 자료를 검증된 출처로 취급하지 않음.

- 완료 응답은 도구 없는 독립 구조화 호출로 원문 발췌만 정보 노드에 추가. 출처 노드는 실제 도구 메타데이터만 사용. 조회/본문 링크 표기와 의미적 근거를 구분. 구조화 실패는 원래 답변에 영향 없이 격리하고 기록 복원 시 추출 호출 금지.

## 개발과 검증
- `make install`, `make dev`, `make check`, `make e2e`를 표준 진입점으로 사용.
- 외부 API 없이 실행되는 테스트 기본. 실제 유료 통합 호출은 명시적 실행.
- 변경 전 git 상태 확인, 사용자 변경 보존. INFO는 요청 수명, DEBUG는 명시적 디버깅 시 청크 크기·모델 설정, ERROR는 안전한 실패 코드·위치만 출력. 키·토큰·질문/응답 원문·내부 추론 기록 금지.
- SDK 추적 기본 비활성화. 요청 예산, 취소, 안전한 링크 검증 우회 금지.

## 원격 / 브랜치 / 커밋 / 푸시
- 원격 `origin`: `https://github.com/rabbitholechat/rabbit-hole.git`.
- `main`: 검증된 통합 브랜치.
- `feat/<kebab-case>`, `fix/<kebab-case>`, `docs/<kebab-case>`, `test/<kebab-case>`.
- 작업 브랜치에서 변경·검증·로컬 커밋 후 `main`에 fast-forward 통합.
- 사용자가 명시적으로 푸시를 지시하기 전에는 어떤 브랜치나 태그도 원격에 푸시하지 않음. 작업·커밋·원격 연결 지시는 푸시 허가가 아님.
- 푸시 지시가 있으면 해당 요청 범위의 브랜치·커밋만 푸시. 강제 푸시·원격 브랜치 삭제·배포는 별도 명시적 요청 범위.
- 현재 PR은 사용하지 않음. 사용자가 작업 방식을 변경하도록 지시하기 전에는 PR 생성 금지.
- 커밋은 아래 형식, 제목과 항목은 명사형(~구성, ~구현, ~검증).

```text
feat: 변경 요약 구성

- 작업 내용 구현

# 영향받은 파일
- 경로

# 검증
- 실행한 검증과 결과
```
